import socket

# import pypyodbc

# SQLServer = "localhost"
# Database = "phonebook"

# connection = pypyodbc.connect('Driver={SQL Server};'
#                               'Server=' + SQLServer + ';'
#                               'Database=' + Database + ';')

# cursor = connection.cursor()
